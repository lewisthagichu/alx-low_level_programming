#include "main.h"

/**
* _abs - computes the absolute value of an integer
*
* @n: int to be checked
*
* Return: the abs value of int
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
